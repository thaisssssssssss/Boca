#include <stdio.h>

int main () {

char letra;
  scanf("%c", &letra);

if (letra == 65 || letra == 69 || letra == 73 || letra == 79 || letra == 85 || letra == 97 || letra == 101 || letra == 105 || letra == 111 || letra == 117) {
  printf("Vogal", letra);
}
else {
  printf("Nao vogal");
}

return 0;  
}